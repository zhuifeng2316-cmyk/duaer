---
description: Spec-driven development for Duaer
alwaysApply: true
---

# Spec-Driven Development（强制）

开发与测试必须遵守 `.specify/memory/constitution.md`。没有 feature 的 `spec.md` / `plan.md` / `tasks.md`，不得直接大规模写业务代码。

## 产品核心

主路径：**人不出镜的 AI 口播**。人物照片 + 可复用克隆音色 → 流式写文案并确认 → 写图片分镜（场景/图种/组法，中文画面动作由系统对组件并填参数）并确认 → 按描述出图库并对照重做 → 生成口播 → 生成配乐 → 按组法用内置合成稿拼图库。屏幕字按信息流写够大。一步出大片，不做时间轴微调。题材含故事口播 / 产品介绍 / 知识输出。单人默认画面里不要出现其他人，除非分镜文字要求或点选了多个角色。
禁止把视频模型出片、对嘴型数字人抬成默认成片方式。C 端不写厂商名。克隆静帧走火山方舟，文案/分镜/口播/配乐走点物 Flow。成片优先用 HyperFrames 渲 `compose/index.html`，失败回退 ffmpeg。音色只支持在线录音克隆，不支持上传。

## 流程

`specify` → `plan` → `tasks` → `implement`。涉及 UI 时对照真实产品（剪映图文成片 / CapCut 比例 / Photos Memories）。

## 三层测试

单元 + 接口 + Cursor 浏览器。有前端改动不可跳过浏览器。
