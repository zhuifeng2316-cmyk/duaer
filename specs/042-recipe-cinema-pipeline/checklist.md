# Checklist: 配方组件主流程大片验收

状态：`未测` | `干跑通过` | `成片通过` | `不合格` | `跳过(原因)`

更新：2026-09-11 流程图嵌套空白已修（template + 满屏 overlay + 首帧可见）；助手组件推荐已推。

## 故事

| 动作 | 干跑 | 成片/抽帧 | 备注 |
|------|------|-----------|------|
| 砸字 | 干跑通过 | 成片通过 | 如果由我 |
| 杂志字 | 干跑通过 | 成片通过 | captionStyle 杂志字短屏；封面 magazine 亦过 |
| 字重切换 | 干跑通过 | 跳过(干跑即可) | 原生字层 + captionStyle |
| 手写标题 | 干跑通过 | 跳过(干跑即可) | |
| 划重点 | 干跑通过 | 成片通过 | 如果由我镜3 金条亮项 |
| 打字机 | 干跑通过 | 跳过(干跑即可) | |
| 标题卡 | 干跑通过 | 跳过(干跑即可) | 封面 titlecard-lockup 已抽 |
| 故障字 | 干跑通过 | 成片通过 | 空→砸字 |
| 漏光 | 干跑通过 | 跳过(干跑即可) | 不烤静帧 |
| 闪白 | 干跑通过 | 跳过(干跑即可) | 不烤静帧 |

## 知识

| 动作 | 干跑 | 成片/抽帧 | 备注 |
|------|------|-----------|------|
| 流程图 | 干跑通过 | 成片通过 | `flowchart_*.jpg`；中文树；首帧可见 |
| 手绘流程 | 干跑通过 | 跳过(干跑即可) | |
| 图表（横） | 干跑通过 | 跳过(干跑即可) | |
| 图表（竖→划重点） | 干跑通过 | 成片通过 | 晚上乱花钱镜3 |
| 数字跳动 | 干跑通过 | 成片通过 | `count-up_*.jpg`；金字 100% |
| 划重点 | 干跑通过 | 成片通过 | 同上 |
| 砸字 | 干跑通过 | 成片通过 | 晚上乱花钱镜1 |
| 代码演示 | 干跑通过 | 成片通过 | `code-run_*.jpg`；中文「出片」 |
| 打勾清单 | 干跑通过 | 成片通过 | 中文壳；host；不叠原生划重点；`checklist_*.jpg` |
| 手写标题 | 干跑通过 | 跳过(干跑即可) | |

## 产品

| 动作 | 干跑 | 成片/抽帧 | 备注 |
|------|------|-----------|------|
| 下拉刷新 | 干跑通过 | 成片通过 | 暗底金条，中文 |
| 通知堆 | 干跑通过 | 成片通过 | `notify-stack_*.jpg`；中文「三条提醒」 |
| 通知弹出 | 干跑通过 | 成片通过 | 中文提醒 |
| 轮播 | 干跑通过 | 成片通过 | cover 铺满；无人物底；槽位循环 |
| 产品展示 | 干跑通过 | 成片通过 | 炭黑底三机位 |
| 大光标 | 干跑通过 | 成片通过 | `big-cursor_*.jpg` |
| 界面放大 | 干跑通过 | 成片通过 | `ui-zoom_*.jpg` |
| 聊天对话 | 干跑通过 | 成片通过 | 中文「朋友 / 先问一句」；`chat-thread_*.jpg` |
| 路径游走 | 干跑通过 | 成片通过 | `path-travel_*.jpg`；「顺着走」 |
| 分享面板 | 干跑通过 | 成片通过 | 中文接受/拒绝；出片前补槽位图；`share-sheet_*.jpg` |

## 字层与皮肤

| 项 | 状态 | 备注 |
|----|------|------|
| 自动砸字 | 成片通过 | |
| 自动擦字短屏 | 成片通过 | |
| 自动划重点 | 成片通过 | 含 `＞` 并列；挂组件时不叠 |
| 杂志字短屏 | 成片通过 | |
| 字重短屏 | 干跑通过 | 跳过(干跑即可) |
| 全片霓虹/粉笔进稿 | 成片通过 | 粉笔字成片；墙上预览霓虹 |
| 单镜覆盖 | 跳过(干跑即可) | |

## 封面

| 模版 | 状态 | 备注 |
|------|------|------|
| 标题卡锁定 | 成片通过 | `cover-lockup.png`；phase 回 done |
| 杂志字封面 | 成片通过 | `cover-magazine.png`；phase 回 done |

## 端到端

| 题材 | 口播 id | 状态 | 备注 |
|------|---------|------|------|
| story | d9a2dd4e-… | 成片通过 | captionStyle=粉笔字；封面 magazine |
| knowledge | d8a936bd-… | 成片通过 | 清单/跳动/代码/流程图过 |
| product | 2dec4575-… | 成片通过 | 宿主烟测齐 |

## 洞

- **轮播 / 产品展示 / 配方外改挂**：已修（见 T11）
- **打勾清单英文壳 / 与划重点叠字**：已修
- **划重点叠人空 imagePrompt**：已修
- **C 端 seek `[0,0]`**：已修（Range + faststart）
- **聊天英文演示壳**：已修（中文 vars + skin）
- **流程图嵌套成片空白**：已修 — house 改 `<template>`+#root；同画幅改满屏 overlay；时间轴首帧即画出节点，去掉收尾闪白
- 杂志字/字重官方组件无变量 → 原生字层 + captionStyle（已接受）
- 手绘流程 / 横版图表：跳过(干跑即可)
