# Checklist: 配方组件主流程大片验收

状态：`未测` | `干跑通过` | `成片通过` | `不合格` | `跳过(原因)`

更新：2026-09-11 T11/T8/T9 收口；抽帧目录 `frames/`。

## 故事

| 动作 | 干跑 | 成片/抽帧 | 备注 |
|------|------|-----------|------|
| 砸字 | 干跑通过 | 成片通过 | 如果由我 |
| 杂志字 | 干跑通过 | 成片通过 | captionStyle 杂志字短屏；封面 magazine 亦过 |
| 字重切换 | 干跑通过 | 跳过(干跑即可) | 原生字层 + captionStyle |
| 手写标题 | 干跑通过 | 跳过(干跑即可) | |
| 划重点 | 干跑通过 | 成片通过 | 如果由我镜3 金条亮项 |
| 打字机 | 干跑通过 | 跳过(干跑即可) | |
| 标题卡 | 干跑通过 | 跳过(干跑即可) | 封面 titlecard-lockup 已抽 |
| 故障字 | 干跑通过 | 成片通过 | 空→砸字 |
| 漏光 | 干跑通过 | 跳过(干跑即可) | 不烤静帧 |
| 闪白 | 干跑通过 | 跳过(干跑即可) | 不烤静帧 |

## 知识

| 动作 | 干跑 | 成片/抽帧 | 备注 |
|------|------|-----------|------|
| 流程图 | 干跑通过 | 跳过(干跑即可) | |
| 手绘流程 | 干跑通过 | 跳过(干跑即可) | |
| 图表（横） | 干跑通过 | 跳过(干跑即可) | |
| 图表（竖→划重点） | 干跑通过 | 成片通过 | 晚上乱花钱镜3 |
| 数字跳动 | 干跑通过 | 跳过(干跑即可) | |
| 划重点 | 干跑通过 | 成片通过 | 同上 |
| 砸字 | 干跑通过 | 成片通过 | 晚上乱花钱镜1 |
| 代码演示 | 干跑通过 | 跳过(干跑即可) | |
| 打勾清单 | 干跑通过 | 跳过(英文壳另修) | |
| 手写标题 | 干跑通过 | 跳过(干跑即可) | |

## 产品

| 动作 | 干跑 | 成片/抽帧 | 备注 |
|------|------|-----------|------|
| 下拉刷新 | 干跑通过 | 成片通过 | 暗底金条，中文 |
| 通知堆 | 干跑通过 | 跳过(干跑即可) | |
| 通知弹出 | 干跑通过 | 成片通过 | 中文提醒 |
| 轮播 | 干跑通过 | 成片通过 | cover 铺满；无人物底；槽位循环 |
| 产品展示 | 干跑通过 | 成片通过 | 炭黑底三机位 |
| 大光标 | 干跑通过 | 跳过(干跑即可) | |
| 界面放大 | 干跑通过 | 跳过(干跑即可) | |
| 聊天对话 | 干跑通过 | 跳过(干跑即可) | |
| 路径游走 | 干跑通过 | 跳过(干跑即可) | |
| 分享面板 | 干跑通过 | 跳过(干跑即可) | |

## 字层与皮肤

| 项 | 状态 | 备注 |
|----|------|------|
| 自动砸字 | 成片通过 | |
| 自动擦字短屏 | 成片通过 | |
| 自动划重点 | 成片通过 | 含 `＞` 并列 |
| 杂志字短屏 | 成片通过 | |
| 字重短屏 | 干跑通过 | 跳过(干跑即可) |
| 全片霓虹/粉笔进稿 | 成片通过 | 粉笔字成片；墙上预览霓虹 |
| 单镜覆盖 | 跳过(干跑即可) | |

## 封面

| 模版 | 状态 | 备注 |
|------|------|------|
| 标题卡锁定 | 成片通过 | `cover-lockup.png`；phase 回 done |
| 杂志字封面 | 成片通过 | `cover-magazine.png`；phase 回 done |

## 端到端

| 题材 | 口播 id | 状态 | 备注 |
|------|---------|------|------|
| story | d9a2dd4e-… | 成片通过 | captionStyle=粉笔字；封面 magazine |
| knowledge | d8a936bd-… | 成片通过 | 图表已改划重点 |
| product | 2dec4575-… | 成片通过 | 轮播+产品展示已过；手势点按不挂 |

## 洞

- **轮播 / 产品展示 / 配方外改挂**：已修（见 T11）
- 打勾清单英文演示字未换中 — 后续
- 杂志字/字重官方组件无变量 → 原生字层 + captionStyle（已接受）
- 静帧烤字 / 空 imagePrompt / C 端 seek 不可靠 — 另修
- 已修：`overlay-highlight` 误成 wipe；`＞` 可解析为划重点项
